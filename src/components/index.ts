import CustomButton from "./CustomButton";
export type ComponentsList = 'CustomButton';

export default {
    CustomButton
}

import buttonConfig from './CustomButton/config.json';

export const CONFIG = {
    CustomButton: buttonConfig,
}